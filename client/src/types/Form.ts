export interface RegisterFormValues {
    name: string;
    phoneNo: string;
    email: string;
    location: string;
    role: string;
}

export interface RawMaterialFormValues {
    name: string;
    quantity: string;
    unit: string;
    code: string;
    buyer: string;
}